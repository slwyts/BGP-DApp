# 环境配置示例
# 复制此文件为 .env.local, .env.development, .env.production 并填写实际值

# WalletConnect Project ID (必需)
NEXT_PUBLIC_PROJECT_ID=your_walletconnect_project_id

# 网络模式 (localnet | development | production)
NEXT_PUBLIC_NETWORK=production

# 合约地址 (根据网络填写)
NEXT_PUBLIC_BGP_TOKEN_ADDRESS=
NEXT_PUBLIC_DAPP_ADDRESS=

# 可选: USDT 地址 (生产环境使用官方地址，测试环境填写部署的地址)
NEXT_PUBLIC_USDT_ADDRESS=

# RPC URL (可选，默认使用公共 RPC)
NEXT_PUBLIC_RPC_URL=
